
<%= simple_form_for @administrator, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Administrator</legend>

    <%= f.input :name %>
    <%= f.input :username %>
    <%= f.input :password %>
    <%= f.input :password_confirmation, :label => "Confirm Password" %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', controller: "admin_sessions", action: "new", :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
