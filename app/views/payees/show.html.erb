<h1>Payee</h1>

<p>
  <b>Name</b><br>
  <%= @payee.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', payees_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_payee_path(@payee), :class => 'btn' %>
  <%= link_to 'Delete', payee_path(@payee), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
